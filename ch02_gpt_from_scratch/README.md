# ch02_gpt_from_scratch

**Status:** not started

**Goal:** Build a character-level GPT from raw PyTorch tensors (embeddings, self-attention, transformer blocks, training loop) on tiny-Shakespeare.

**Contents:**
- `notebook.ipynb` / scripts — code for this chapter (added as we work through it)
- `checkpoints/` — model checkpoints (gitignored)
- Results and key takeaways will be added here after the session, and cross-linked from `RUNBOOK.md`.
